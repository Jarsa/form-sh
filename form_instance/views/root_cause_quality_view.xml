<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- <record model="ir.actions.act_window" id="action_log">
        <field name="name">Root Cause</field>
        <field name="res_model">quality.reason</field>
        <field name="view_mode">tree,form</field>
    </record>

    <record model="ir.ui.view" id="cause_tree_view">
        <field name="name">cause.tree</field>
        <field name="model">quality.reason</field>
        <field name="arch" type="xml">
            <tree string="Root Cause">
                <field name="name"/>
            </tree>
        </field>
    </record>

    <record model="ir.ui.view" id="cause_form_view">
        <field name="name">cause.form</field>
        <field name="model">quality.reason</field>
        <field name="arch" type="xml">
            <form string="Add Root Cause">
                <sheet>
                    <group>
                        <field name="name"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <menuitem name="Add Root Cause" id="add_root_cause" parent="quality_control.menu_quality_configuration" action="action_log"/> -->
</odoo>
